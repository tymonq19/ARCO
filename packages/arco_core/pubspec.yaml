name: arco_core
description: Deterministic simulation, replay and network protocol for Arco (pure Dart, shared by app and server).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
