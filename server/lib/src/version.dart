/// Build identity of the server, reported by `GET /api/health` (SPEC §4).
library;

/// Semantic version of the server binary. Kept in sync with `pubspec.yaml`.
const String serverVersion = '1.0.0';
